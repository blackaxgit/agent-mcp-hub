node_modules/
dist/
*.log
.env

# Internal process docs (plans, fixes, reviews) — kept local, not pushed
docs/

# Subagent scratch memory — local only
.claude/

# test coverage output
coverage/
.stryker-tmp/
reports/
